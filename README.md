# New Apps Script Project

## 🛠 Setup
1. `clasp login --user work`
2. `clasp create --type webapp --title "Project Name"`
3. Update `.clasp.json` to include `"user": "work"`.

## 🚀 Workflow
- `clasp push` to sync code.
- `clasp deploy` to release new versions.
